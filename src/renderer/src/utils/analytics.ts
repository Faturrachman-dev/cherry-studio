/**
 * Analytics stripped from Bob's fork.
 * trackTokenUsage is a no-op stub kept for API compatibility.
 */

export function trackTokenUsage(_params: unknown): void {
  // Intentionally empty - analytics removed
}
